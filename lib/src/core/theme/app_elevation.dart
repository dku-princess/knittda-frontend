/// Knittda elevation(그림자) 토큰.
///
/// Material `elevation` 값을 역할별로 지정한다. 코드 실사용에 있는 값만 둔다.
class AppElevation {
  AppElevation._();

  /// 그림자 없음 (앱바 등).
  static const double none = 0;

  /// 카드·프리뷰 아이템의 떠 있는 그림자.
  static const double card = 7;
}
